# z 的工作台 · 部署与同步教程

> 本工作台为**云端永久存储 + 永久在线**架构：
> - 数据默认存在你浏览器本地（LocalStorage），**双击 index.html 也能用**；
> - 同步后端有三选一（只需其一）：
>   1. **GitHub 仓库同步（推荐，无需另开主机）**：用你的 GitHub 私有仓库当「永远在线」后端，手机/电脑共用一份数据；
>   2. **Firebase 实时数据库**（需能登录 console.firebase.google.com，国内常被屏蔽）；
>   3. **自托管 Node 服务**（`server.js`，需能运行 Node 的主机，如 Render/Railway）。
> - 未配置任何后端时，自动回退「本地模式」，数据不丢、可随时用「导出/导入」迁移。

---

## ① 云端部署上传步骤（让网页永久可访问）

把 `output` 文件夹整体部署到任意免费静态托管平台，**生成专属公开网址，域名永久有效**，不会 12/24 小时过期。

### 方案一：Vercel（推荐，最简单）
1. 注册 https://vercel.com （可用 GitHub 账号登录）
2. 安装 Vercel CLI（电脑终端）：`npm i -g vercel`
3. 进入 `output` 文件夹：`cd output`
4. 执行 `vercel` → 按提示登录 → 一路回车
5. 部署完成会给出一个 `https://xxx.vercel.app` 网址，**这就是你的永久工作台地址**

### 方案二：Netlify Drop（拖拽即可，无需命令行）
1. 打开 https://app.netlify.com/drop
2. 把整个 `output` 文件夹**拖进去**
3. 几秒后生成 `https://xxx.netlify.app` 网址，永久有效

### 方案三：Cloudflare Pages
1. 登录 https://pages.cloudflare.com → 新建项目 → 上传 `output` 文件夹
2. 构建命令留空、输出目录填 `.`（即根目录）
3. 部署完成得到 `https://xxx.pages.dev` 网址

> ✅ 部署后，**任何设备浏览器打开该网址即可使用**，只要托管服务正常就永远打不开失败。

---

## ② Firebase 免费数据库搭建配置教程（云端实时同步核心）

> Firebase 实时数据库（Realtime Database）有免费额度，足够个人长期使用。

### 第 1 步：创建项目
1. 打开 https://console.firebase.google.com
2. 点「添加项目」→ 填名称（如 `z-workbench`）→ 一路「继续」创建（可关掉 Google Analytics）

### 第 2 步：开启 Realtime Database
1. 左侧「构建」→「Realtime Database」→「创建数据库」
2. 地区选离你近的（如 `asia-southeast1` 新加坡）
3. 先以「测试模式」启动（方便调试）

### 第 3 步：设置数据库权限规则
进入「Realtime Database → 规则(Rules)」，替换为：
```json
{
  "rules": {
    "users": {
      "$uid": {
        ".read": true,
        ".write": true
      }
    }
  }
}
```
> 这是个人工作台，规则开放便于多端同步。若担心被他人猜到 uid 改数据，可后续改成 Firebase 匿名登录校验（进阶）。

### 第 4 步：获取 Web 配置
1. 项目设置（⚙️ 齿轮）→「你的应用」→「添加应用」→ 选 `</>`（Web）
2. 注册应用后，复制给出的 `firebaseConfig` 里的 5 个值

### 第 5 步：填进代码
打开 `output/firebase-config.js`，把占位符替换成你的真实值：
```js
const firebaseConfig = {
  apiKey:            "AIzaSxxxx",          // 你的真实值
  authDomain:        "z-workbench.firebaseapp.com",
  databaseURL:       "https://z-workbench-default-rtdb.firebaseio.com",
  projectId:         "z-workbench",
  appId:             "1:123:web:abc"
};
```
保存后**重新部署**（Vercel/Netlify 重新上传即可）。

### 第 6 步：验证
打开网页 → 看左侧栏底部「同步模式」徽标：
- 显示「云端已连接」→ 配置成功，数据已写入 Firebase
- 显示「本地模式」→ 配置未生效，检查占位符是否替换、是否重新部署

---

## ③ 手机添加至桌面做成 App（PWA）

### 🍎 iPhone（Safari）
1. 用 Safari 打开你的云端网址（如 `https://xxx.vercel.app`）
2. 点底部 **分享按钮** 📤 → 滑到「**添加到主屏幕**」
3. 名称默认 `z工作台`，点右上角「添加」
4. 桌面出现图标，点开**全屏启动、无地址栏**，和原生 App 一样

### 🤖 安卓（Chrome / Edge）
1. 浏览器打开云端网址
2. 点右上角菜单（⋮）→「**添加到主屏幕**」或「**安装应用**」
3. 确认后桌面生成图标，点击全屏启动
4. App 依托云端网址运行，只要托管服务正常就**永久可用**，不会随浏览器失效

> 💡 必须部署到云端网址后再添加桌面，才能「永久可访问」。直接打开本地 html 文件添加的图标，换设备/清缓存会失效。

---

## ④ 多端数据实时同步使用方法

### 原理
- 所有数据按「数据身份标识（uid）」存放在 Firebase，例如 `z-a1b2c3d4`
- 同一标识在手机、电脑、平板上打开 → **共享同一份数据**
- 任意一端签到 / 勾选 / 新增 → 毫秒级写入云端 → 另一端实时刷新

### 操作步骤
1. 在**第一部设备**（如电脑）打开已部署的网页，正常使用几天
2. 点左侧栏底部「**同步模式**」徽标 → 复制显示的标识（如 `z-a1b2c3d4`）
3. 在**第二部设备**（如手机）打开同一云端网址
4. 同样点「同步模式」徽标 → 在弹窗里**粘贴第一步的标识** → 确认
5. 页面自动刷新，第二部设备立即出现第一部设备的全部历史数据
6. 之后两端各自修改，都会实时互相同步 ✅

### 注意事项
- 更换手机 / 清除浏览器缓存：只要重新贴入同一标识，历史数据全部取回
- 离线时：自动用本地缓存兜底，联网瞬间自动同步云端最新记录
- 数据永久留存于 Firebase，不会自动清除（除非你手动删除数据库节点）

---

## ⑤ GitHub 仓库同步（推荐 · 永远在线 · 无需另开主机）

> 适合无法登录 Firebase（国内 Google 被屏蔽）的情况。用一个 GitHub 私有仓库当同步后端，
> 纯浏览器端读写，**电脑关机、网页托管在哪都不影响同步**。

### 第 1 步：建一个私有仓库
1. 打开 https://github.com （用邮箱注册，非 Google 账号）
2. 右上角「New」新建仓库，名字如 `z-workbench-sync`
3. 选 **Private（私有）**，勾不勾 README 都行 → Create

### 第 2 步：生成个人访问令牌（PAT）
1. 右上角头像 → **Settings** → 左侧 **Developer settings** → **Personal access tokens** → **Tokens (classic)**（或 Fine-grained）
2. 点 **Generate new token** → 勾选 `repo` 权限（其余全空）→ 生成
3. **复制那串 `ghp_xxx` / `github_pat_xxx` 令牌**（只显示这一次，妥善保存）

### 第 3 步：在工作台里填好
1. 打开你的云端工作台网址
2. 点左侧栏底部「**同步模式**」徽标（默认显示「本地模式」）→ 弹出 GitHub 设置
3. 填入：
   - **PAT**：刚才复制的令牌
   - **仓库**：`你的用户名/z-workbench-sync`
   - **文件路径**：默认 `z-workbench-sync.json` 即可
4. 点「**保存并同步**」
5. 徽标变成「**GitHub 同步中**」即成功；首次会自动把本机数据推上仓库

### 第 4 步：第二台设备
在手机/平板上打开**同一网址** → 同样点徽标 → 填**同一个 PAT 和仓库** → 保存。
两端约 6 秒互相同步，一份数据永远在线 ✅

### 注意事项
- 令牌只在你本机浏览器 LocalStorage 保存，**不会上传到任何服务器**；
- 想停用：点徽标 → 「清除」即可；
- GitHub API 有频率上限（认证后 5000 次/小时），个人使用远不会触顶；
- 换设备/清缓存：重新填一次 PAT+仓库，历史数据全部取回。

---

## 📁 文件清单

```
output/
├── index.html        主页面
├── styles.css        蓝系渐变样式
├── app.js            主逻辑（数据层 + 渲染 + 云端同步）
├── firebase-config.js Firebase 配置（填真实值启用云端）
├── sw.js             Service Worker（仅桌面图标封装，联网优先）
├── manifest.json     PWA 配置（添加到桌面用）
├── avatar.jpg        头像 / App 图标
└── 使用教程.md       本文件
```

祝你稳步前行，每一天都超越昨天 ✦
